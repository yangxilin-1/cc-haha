{
  "$schema": "https://raw.githubusercontent.com/nicegui/nicegui/main/nicegui/static/tauri-capabilities-schema-v2.json",
  "identifier": "default",
  "description": "Default capabilities for Ycode Desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "args": [
            "server",
            { "validator": "regex", "value": "^(--port|--host|--config)=.+$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        },
        {
          "args": [
            "adapters",
            { "validator": "regex", "value": "^(feishu|telegram|lark|slack)(:.+)?$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        }
      ]
    },
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "args": [
            "server",
            { "validator": "regex", "value": "^(--port|--host|--config)=.+$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        },
        {
          "args": [
            "adapters",
            { "validator": "regex", "value": "^(feishu|telegram|lark|slack)(:.+)?$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        }
      ]
    },
    {
      "identifier": "shell:allow-kill",
      "allow": [
        {
          "args": [
            "server",
            { "validator": "regex", "value": "^(--port|--host|--config)=.+$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        },
        {
          "args": [
            "adapters",
            { "validator": "regex", "value": "^(feishu|telegram|lark|slack)(:.+)?$" }
          ],
          "name": "binaries/ycode-sidecar",
          "sidecar": true
        }
      ]
    },
    "dialog:allow-open",
    "dialog:allow-save",
    "process:allow-exit",
    "process:allow-restart",
    "updater:default"
  ]
}
